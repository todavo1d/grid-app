{
  "version": "0.0.0",
  "private": true,
  "name": "grid-app",
  "devDependencies": {
    "http-server": "^0.6.1",
    "bower": "^1.3.1"
  },
  "scripts": {
    "start": "http-server -a 0.0.0.0 -p 8000",
    "postinstall": "bower install",
    "prestart": "npm install"
  }
}
